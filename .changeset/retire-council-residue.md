---
"@appsforgood/next-supabase-kit": minor
---

Cursor Task types are the agent ids; `update --prune-legacy` removes 0.3 council leftovers after confirmation and `doctor` fails on a council stub shadowing a 0.4 agent. One ask-before-acting policy and one set of spawn payloads live in `catalog.json` and `AGENTS.md`. `frontend-design` derives tokens from the product and owns the visual fail list; `deslop` is copy-only with structure tells; shipped skills carry none of the kit's own palette.
